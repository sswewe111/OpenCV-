# 读取和处理、ROI区域、边界填充、数值计算

## 读取和处理

```python
import cv2

#读取图像
img=cv2.imread("xxx.jpg")

#显示图像-第一个是窗口名称，第二个是读取的图像
cv2.imshow("xxx",img)
#等待时间，0表示任意键终止
cv2.waitKey(0)
cv2.destroyAllWindows()

#图像保存
cv2.imwrite("xxx.png"，img)

#视频读取
#1.捕获视屏
vc=cv2.videoCapture("xxx.mp4")

#判断视频是否能打开，True表示能打开
vc.isOpened()

#逐帧读取视频
wihle True:
		ret,frame=vc.read()
		if frame is None:
				break
		if ret==True:
				gray=cv2.cvtColor(frame,cv2.COLOR_BGR2GRAY)
				cv2.imshow("xxx",gray)
				if cv2.waitKey(1) & 0xFF == 27
						break

#调整图像尺寸
img=cv2.resize(img,(num1,num2))

```

## ROI区域

```python
#截取部分图像数据
cat=img[0:200,0:200]

#颜色通道提取
b,g,r=cv2.split(img)

#合并颜色通道
img = cv2.merge((b,g,r))

#如何修改某个通道数
#将指定通道数置0
img[:,:,0]=0
img[:,:,1]=0
img[:,:,2]=0

```

## 边界填充

```python
top_size, bottom_size, left_size, right_size = (50, 50, 50, 50)

• BORDER_REPLICATE：复制法，也就是复制最边缘像素。
replicate = cv2.copyMakeBorder(
    img, top_size, bottom_size, left_size, right_size,
    cv2.BORDER_REPLICATE
)

- BORDER_REFLECT：反射法，对原图像中的像素在两边进行复制例如：fedcba|abcdefgh|hgfedcb
reflect = cv2.copyMakeBorder(
    img, top_size, bottom_size, left_size, right_size,
    cv2.BORDER_REFLECT
)

- BORDER_REFLECT_101：反射法，也就是以最边缘像素为轴，对称，gfedcb|abcdefgh|gfedcba
reflect101 = cv2.copyMakeBorder(
    img, top_size, bottom_size, left_size, right_size,
    cv2.BORDER_REFLECT_101
)

- BORDER_WRAP：外包装法 cdefgh|abcdefgh|abcdefg
wrap = cv2.copyMakeBorder(
    img, top_size, bottom_size, left_size, right_size,
    cv2.BORDER_WRAP
)

- BORDER_CONSTANT：常量法，常数值填充。
constant = cv2.copyMakeBorder(
    img, top_size, bottom_size, left_size, right_size,
    cv2.BORDER_CONSTANT,
    value=0
)
```

## 数值计算

```python
#对所有位置进行加减乘除等数值操作
int num
img=img +-*/ num

#图像融合,前提是融合的两张图片的的尺寸一致
img=img_cat+img_dog
```